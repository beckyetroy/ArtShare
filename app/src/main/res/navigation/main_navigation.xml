<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_navigation"
    app:startDestination="@id/viewFragment">

    <fragment
        android:id="@+id/createFragment"
        android:name="wit.mobileappca.artshare.fragments.CreateFragment"
        android:label="@string/menu_createEdit"
        tools:layout="@layout/fragment_create" >
        <action
            android:id="@+id/action_createFragment_to_viewFragment"
            app:destination="@id/viewFragment"
            app:enterAnim="@anim/nav_default_pop_enter_anim"
            app:exitAnim="@anim/nav_default_pop_exit_anim" />
    </fragment>
    <fragment
        android:id="@+id/viewFragment"
        android:name="wit.mobileappca.artshare.fragments.ViewFragment"
        android:label="@string/menu_view"
        tools:layout="@layout/fragment_view" >
        <action
            android:id="@+id/action_viewFragment_to_createFragment"
            app:destination="@id/createFragment"
            app:enterAnim="@anim/nav_default_pop_enter_anim"
            app:exitAnim="@anim/nav_default_pop_exit_anim" />
    </fragment>
</navigation>